---
sidebar_position: 1
---

# Licen 产品介绍

## 产品定位

Licen 是一套**私有化部署产品授权服务**，为交付到客户现场的私有化软件（AI 引擎、应用服务、边缘网关等）提供开箱即用的授权管理方案：**先部署后激活**、硬件绑定、节点并发控制、功能点授权、防篡改防逆向，一条龙搞定。

它解决的核心问题是：软件交付到客户内网后，"谁能用、用几台、用多久、用哪些功能"由谁说了算——答案是**厂商**。客户不续费，License 到期自动失效，无需上门卸载。

:::tip 一句话概括

厂商用私钥签发 License（绑定客户机器码），客户侧授权服务（licen-server）用内置公钥验签，通过后才开放注册/心跳等业务能力。整个过程可完全离线运行。

:::

## 项目优势

与传统自研/商业授权方案相比，Licen 在以下维度具有明显优势：

| 维度 | Licen | 传统自研/商业方案 |
|---|---|---|
| **交付体验** | **先部署后激活**：客户先装好系统，机器码发给厂商，一键签发、上传即激活，无需等待 | 一般需现场安装时同步授权，或邮寄加密狗（U 盾），周期长 |
| **部署形态** | `licen-server` 单二进制 + SQLite，**零外部依赖**（不依赖 MySQL/Redis/Nginx），拷到客户 VM 即跑 | 通常要配套数据库、中间件，实施成本高 |
| **防拷贝** | License 绑定**硬件指纹锚点**（系统UUID→磁盘→MAC→主板→CPU 优先级取非空）SHA-256 机器码，加盘/换网卡/调 vCPU/VM 迁移都不变码，整套拷走也跑不起来 | 单纯 License 文件可随意复制 |
| **防篡改** | RSA-2048 私钥签名，改一个字段即 `INVALID_SIGNATURE` | 无签名或弱校验 |
| **防逆向** | Go 静态编译 + 去符号 + **garble 混淆**（每次构建结果不同，防版本比对） | 解释型/字节码易反编译 |
| **并发控制** | License 限定最大并发节点数，心跳保活、超时**自动回收**名额，防僵尸占用 | 无节点级管控 |
| **多语言 SDK** | Java / Go / Python / C **四语言官方 SDK**，统一协议契约，接入 5 分钟 | 通常只有单一语言 SDK |
| **运维便利** | 机器码采集、License 热重载、管理 API、审计日志，Web 签发中心 | 手工改配置、重启服务 |
| **成本** | 纯 Go 开源，无按量计费、无云依赖，可完全离线运行 | 按节点/按年收费的授权网关 |

## 功能截图

### 厂商签发中心（licen-issuer）—— 输入机器码，一键签发

签发中心是面向客服/运营的 Web 工具：输入客户机器码，选择产品与有效期，即可一键生成 License 文件供下载回传。

| 签发界面 | 填写客户信息 |
|---|---|
| ![签发界面](/img/licen/01-issuer-webui.png) | ![填写客户信息](/img/licen/02-issuer-form-filled.png) |

| 签发结果（含 License 全文 + 下载） |
|---|
| ![签发结果](/img/licen/03-issuer-result.png) |

### 客户侧授权服务（licen-server）—— 激活后全功能

客户侧内置 **Web 管理平台**（`GET /admin`，零依赖单文件），授权状态 / 节点 / 应用 / 审计一站式可视化：

| 管理平台总览（授权状态 + 节点 + 应用 + 审计） | 全页视图（长截图） |
|---|---|
| ![管理平台总览](/img/licen/06-server-admin-platform.png) | ![全页视图](/img/licen/07-server-admin-nodes-full.png) |

| 授权状态（VALID / 节点数 / 功能点） | 节点管理（并发节点在线） |
|---|---|
| ![授权状态](/img/licen/04-server-license-status.png) | ![节点管理](/img/licen/05-server-admin-nodes.png) |

## 应用场景

### 1. 私有化软件交付（最典型）

AI 推理引擎、NLP 平台、数据中台等产品交付到客户内网时，用 Licen 控制"谁能用、用几台、用多久、用哪些功能"。客户不续费 → 到期自动失效，无需上门卸载。

### 2. 边缘网关 / 嵌入式设备

C SDK 支持**纯 socket 零依赖**模式，可嵌入 ARM 边缘网关等受限环境；每台设备一个 License，绑定硬件防刷机盗用。

### 3. SaaS 转私有化

把 SaaS 产品打包交付给大客户私有化部署时，用 Licen 做**订阅管理**：按年授权、按并发节点数计费、按功能点区分标准版/旗舰版。

### 4. 项目型软件（集成商/代理）

集成商代理多套软件时，用 Licen 统一管理所有客户的授权：哪家客户、几个节点、何时到期，Web 签发中心一目了然，不用记文件、查邮件。

### 5. 试用版 / PoC 引流

签发 30 天试用 License 给潜在客户，到期自动停；客户体验满意再买正式版，厂商远程签发续期即可（`/api/v1/activate` 热更新）。

## 系统组成

| 组件 | 角色 | 说明 |
|---|---|---|
| **licen-tool** | 厂商 CLI | `gen-keypair` / `gen-license` / `verify` / `machinecode`，离线签发 |
| **licen-issuer** | 厂商 Web 签发服务 | Web UI + REST API：机器码 → license.json，已签发授权全部留痕 |
| **licen-server** | 客户侧授权服务 | Go 单二进制 + SQLite，先部署后激活，验签/机器码/有效期/节点并发控制 |
| **`licen-sdk-{java,go,python,c}`** | 产品接入 SDK | 四语言官方 SDK，统一协议契约，注册/心跳/宽限期/自愈/能力校验 |

## 总体架构

```
┌────────────────────────────────────────────────┐
│ 厂商侧（内网）                                   │
│  licen-tool (Go CLI)                           │
│    gen-keypair / gen-license / verify / machinecode │
│  licen-issuer (Web 签发服务)                    │
│    Web UI + REST API：机器码 → license.json     │
│    私钥自留，公钥内置到 licen-server             │
└───────────────┬────────────────────────────────┘
                ▼ License 文件（JSON + RSA 签名，绑定机器码）
┌────────────────────────────────────────────────┐
│ 客户侧（专用 VM）                               │
│  licen-server (Go 单二进制 + SQLite)            │
│    先部署后激活：机器码采集 → 上传 License → 解锁  │
│    验签 → 机器码匹配 → 有效期 → 节点并发控制      │
│    REST API：激活 / 注册 / 心跳 / 状态查询 / 管理 │
└───────────────┬────────────────────────────────┘
                ▲ 注册/心跳（REST + HMAC 签名）
   ┌────────────┼───────────┬───────────┐
 licen-sdk    licen-sdk    licen-sdk   licen-sdk
 -java(已有)   -python      -go         -c
```

厂商侧私钥自留，公钥内置到 licen-server 二进制；License 文件（JSON + RSA 签名，绑定机器码）是两侧之间唯一的交付物。客户产品通过四语言 SDK 与 licen-server 交互（注册/心跳），由 licen-server 统一执行授权校验。
