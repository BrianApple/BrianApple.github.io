---
sidebar_position: 3
---

# 架构设计

IOTGate 网关与前置（master）通信时，通过**自定义网关头报文**完成网关登录与真实报文传输。网关头携带网关自身和终端响应参数，报文结构由网关自行定义，前置按照定义好的报文格式获取数据并做相应处理。

## 网关头结构

网关头共 10 个字段，结构如下：

| 报文属性 | 字节数 | 含义 |
|---|---|---|
| `AB` | 1 byte | 报文头 |
| `len` | 4 byte | 长度域：真实报文长度，包含"68"，"16" |
| `type` | 1 byte | 报头类型 |
| `protocolType` | 1 byte | 协议类型（左侧起第一个 bit 为 0 表示 IPV4，1 表示 IPV6，剩余 7 个 bit 表示规约类型编号） |
| `gateNum` | 1 byte | 网关编号 |
| `00*12` | 12 byte | 如果 ip 格式为 IPV4，则当前为 12 字节 0；反之，当前得 12 个 byte + 后续得 4 byte 存放 IPV6 的值，存放顺序从左至右依次 |
| `clientIP` | 4 byte | 终端的 IP 地址，ip 地址的每个段位占一个字节（不含符号和端口号） |
| `port` | 2 byte | 终端对应的端口号 |
| `count` | 4 byte | 终端与网关建立连接时对应的连接序号（1-10000 循环） |

## 登录报文规则

网关发送需要向前置发送**登录报文**，将自己注册到前置服务中，报文说明如下：

1. 登录时长度为 0；有真实报文时，长度域为整个真实报文长度值
2. 登录时 `type = 03`、`protocolType = 15`、`count = 1` 都为固定值；发送真实报文时，`type = 01`、`protocolType = 00`、`count = 终端与网关的连接序号`
3. 前置发现报头长度为 0 且 `type = 03`、`protocolType = 15`，就不会执行解析数据的方法，否则会继续解析真实报文

### 通信流程

```text
感知终端 --(真实报文)--> IOTGate 网关 --(网关头 + 真实报文)--> 前置(master) --(解析/转发)--> 上层应用
                              |--(登录报文 type=03)--> 前置注册
                              |--(10s 心跳 + 404 自愈重注册)--> IOTGateConsole (可选)
```

## GATE CLUSTER 三层结构

![集群版 IOTGate 架构](/img/iotgate/cluster-arch.jpeg)

GATE CLUSTER 由三层组成：

- **感知终端**：物联感知设备，与网关建立长连接，上报心跳与真实报文
- **GATE（IOTGate 网关）**：终端接入层，支持单机与集群部署，通过 `-m` 直连前置，集群版默认心跳周期 300 秒
- **GATE CLIENT（IOTGateConsole）**：Spring Boot 3.5 Web 工程（项目地址：https://gitee.com/willbeahero/IOTGateConsole ），用户登录之后可以查看当前 GATE CLUSTER 的运行状态监控，并可执行网关规约解析服务的启动、关闭、新增、删除等操作

各层职责：

| 层级 | 组件 | 职责 |
|---|---|---|
| 接入层 | 感知终端 | 建立长连接，按规约上报心跳与真实报文（modbus TCP / IEC 104 / DLT 645 等） |
| 网关层 | IOTGate | 终端接入与报文解析，网关头组包，登录前置，集群负载均衡（轮询），心跳保活 |
| 管理层 | IOTGateConsole | 节点监控、规约启停管理、AI 智能体、SSE 实时推送 |

:::note
图中 GATE CLIENT（项目名称"IOTGateConsole"）即管理平台，与网关通过 `-c -r` 主动注册模式联动：HTTP 注册 + 10s 心跳 + 404 自愈重注册。
:::

## kernel 服务模式

IOTGate 最新支持与 master 节点通信的 **kernel 模式**。下图中，红色实线框部分为 **normal 部署模式**，黑色虚线框部分为最新支持的 **kernel 模式**，即 master 节点和所有的感知终端统一作为与 IOTGate 服务网络通信的客户端服务：

![kernel 服务模式](/img/iotgate/kernel-mode.png)

| 部署模式 | 说明 |
|---|---|
| **normal 模式** | master 节点作为服务端，IOTGate 与感知终端作为客户端接入（红色实线框部分） |
| **kernel 模式** | master 节点和所有感知终端统一作为与 IOTGate 服务网络通信的客户端（黑色虚线框部分），通过 `-k` 命令行参数开启，默认端口 10915 |

## 端口规划

| 端口 | 用途 |
|---|---|
| **10915** | kernel 模式默认端口（`-k` 开启） |
| **10916** | rpc 通信（集群模式下开启，Console 经此端口调用网关规约启停等 RPC） |
| **8888** | 前置(master)数据通道（`-m` 参数直连） |

:::tip
集群模式下，网关与前置通讯默认采用**轮询方式负载均衡**；v2.0 起去除 zookeeper 依赖，通过 `-m` 逗号分隔直连多个前置，数据通道零改动。
:::
